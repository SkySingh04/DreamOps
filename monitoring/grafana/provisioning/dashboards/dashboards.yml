apiVersion: 1

providers:
  - name: 'DreamOps'
    orgId: 1
    folder: 'DreamOps'
    folderUid: 'dreamops'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
